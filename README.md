# Practice7

A Python portfolio project encompassing three explicit independent Pygame exercises designed explicitly towards structured beginner-friendly patterns matching the assigned tasks.
1. **Mickey's Clock Application**: A synchronized visualization generating real-time analogue time updates displaying rotating arms representing intervals safely.
2. **Music Player with Keyboard Controller**: A structural system operating multimedia logic processing user states adjusting tracks securely reading directories manually.
3. **Moving Ball Game**: A boundary-aware interactive program controlling a visual red ball model bounded internally against edge overlaps driven by user arrow keys.

## Setup Instructions

### Pre-requisites
- Application utilizes `Python 3` specifically enforcing minimal dependency complexities natively.

### Installation
1. Navigate directly to the foundational `Practice7` parent folder containing sub-project folders.
2. Provide requirements explicitly to the package manager resolving constraints globally using the subsequent setup:
   ```bash
   pip install -r requirements.txt
   ```

## Running Directives

- **Task 1: Mickey's Clock**
  Operates without dependencies directly from folder constraints representing simple time tracking natively.
  ```bash
  cd mickeys_clock
  python main.py
  ```

- **Task 2: Music Player**
  Before starting, deposit target `.mp3` or `.wav` compatible multimedia assets straight into the `music_player/music/sample_tracks/` generated directory manually.
  ```bash
  cd music_player
  python main.py
  ```

- **Task 3: Moving Ball**
  Starts directly parsing arrow inputs checking standard boundaries structurally mapping geometric updates.
  ```bash
  cd moving_ball
  python main.py
  ```

## Requirements Verification Overview
- Every task encompasses a separate self-sufficient `main.py` entry point configured safely using conventional protections (`if __name__ == '__main__':`) validating requirements properly.
- Solutions were specifically optimized avoiding external overhead providing clean portable structural abstractions safely decoupled per explicit directions. Errors, missing multimedia, directories and context faults are explicitly mapped gracefully circumventing crash states implicitly. 
- Refer strictly towards internal sub-folder documentation reading (`README.md`) checking granular validation logic covering target application parameters.
